<%= form_with(model: bid_management, local: true) do |form| %>
  <% if bid_management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bid_management.errors.count, "error") %> prohibited this bid_management from being saved:</h2>

      <ul>
      <% bid_management.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :bid_management_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
